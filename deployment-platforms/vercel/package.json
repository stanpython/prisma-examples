{
  "name": "prisma-vercel",
  "dependencies": {
    "@prisma/client": "2.5.1"
  },
  "scripts": {
    "start": "node index.js",
    "prisma:introspect": "prisma introspect",
    "prisma:generate": "prisma generate",
    "postinstall": "prisma generate"
  },
  "devDependencies": {
    "@prisma/cli": "2.5.1",
    "vercel": "20.0.0"
  }
}
